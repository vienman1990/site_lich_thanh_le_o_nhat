---
const { item } = Astro.props;

function formatJapaneseDate(dateString: string) {
  const date = new Date(dateString);
  const month = date.getMonth() + 1; // getMonth() trả về 0-11, nên +1
  const day = date.getDate();
  const year = date.getFullYear();
  const weekdayIndex = date.getDay(); // 0 (Chủ nhật) đến 6 (Thứ bảy)

  // Mảng thứ bằng tiếng Nhật
  const weekdays = ["日", "月", "火", "水", "木", "金", "土"];
  const weekday = weekdays[weekdayIndex];

  return [day, month, year, weekday];
}

var date = formatJapaneseDate(item.date);

---

<div class="border-b border-purple-300">
  <div class="text-xl my-5">
    Ngày { date[0] }
    tháng { date[1] }
    năm { date[2] }
    ({ date[3] })
  </div>
  <div class="text-3xl my-5">{ item.title }</div>
  <div class="text-xl my-5">{ item.name }</div>
  <div class="text-xl my-5">Thánh Lễ bắt đầu vào giờ: { item.time }</div>
  <div class="text-xs my-5">* ngày và giờ Thánh Lễ có thể bị thay đổi, vui lòng liên hệ cộng đoàn địa phương để xác nhận trước.</div>
</div>
